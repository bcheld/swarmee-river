# third_party

This directory is for local, non-committed checkouts used during development (e.g. pinning an upstream repo for parity work).

By default, `third_party/opencode/` is ignored so you can clone the OpenCode repository here without committing it.

